"""
Tests - pytest suite mirroring the source package structure.

Test modules are named test_<module>.py to match the source file
they cover. This makes it obvious which tests belong to which code.
"""
