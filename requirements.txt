# ======================
# Requirements (thin wrapper)
# ======================
# All dependencies are now defined in pyproject.toml.
# This file exists for backwards compatibility with tools and CI systems
# that expect a requirements.txt to exist.
#
# Usage:
#   pip install -e ".[dev]"    <-- installs runtime + dev/test dependencies
#   pip install -e .           <-- installs runtime dependencies only
#
# If you must use this file directly:
#   pip install -r requirements.txt
#
-e ".[dev]"
