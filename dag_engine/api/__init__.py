"""
API Layer - FastAPI routes, request/response schemas, and dependency injection.

This package handles all HTTP concerns. It receives requests, validates input,
delegates to the core logic layer, and returns responses. No business logic
should live here.
"""
